# include <stdio.h>
int main(){ //int:一定要回傳整數 void:不回傳值
    // data type
    //int(%d), char(%c)(只能有一個字元,用單引號 ex.'e'), float(%f)(小數點只能有6位), bool, double(%f)(小數點能有12位)
    int h,a;
    a = 10;
    h = 345;
    printf("hello world %d",a);
    return 0;
}